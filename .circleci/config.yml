version: 2.1
executors:
  my-executor: 
    docker:
      - image: circleci/node:latest
jobs:
  build:
    executor: my-executor
    steps:
      - run: echo "A first hello"
  # two:
  #   executor: my-executor
  #   steps:
  #     - run: echo "A more familiar hi"
