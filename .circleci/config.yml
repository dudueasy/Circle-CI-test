version: 2.1
executors:
  my-executor: 
    docker:
      - image: circleci/ruby:2.4.1

jobs:
  build:
    executor: my-executor
    steps:
      - run: echo "A first hello"
  # two:
  #   executor: my-executor
  #   steps:
  #     - run: echo "A more familiar hi"
